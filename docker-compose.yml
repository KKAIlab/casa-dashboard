services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - casa-data:/app/data
    environment:
      - PYTHONPATH=/app

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  casa-data:
