# The dashboard is a pure client-side static site (no backend).
# This compose file is only for self-hosting the built frontend; the
# public deployment is served from GitHub Pages.
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
