server {
    listen 3000;
    root /usr/share/nginx/html;
    index index.html;

    # Pure client-side app: no backend. SPA fallback so deep links work.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
