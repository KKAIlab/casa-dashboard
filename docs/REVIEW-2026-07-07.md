# CASA Dashboard — Comprehensive Systematic Review

**Date:** 2026-07-07
**Scope:** Full repository — client-side analysis engine, React UI, IndexedDB layer,
legacy FastAPI backend, deployment, tests, and dependency hygiene.
**Method:** Static reading of every source file, plus `vitest run`,
`eslint`, and a manual numeric audit of the statistical routines.

> **Update (2026-07-07, follow-up passes):** the low-risk, unambiguous findings
> below have been fixed on this branch (H1, H3-labeling, H4, M2, M3, D1, P2, P3,
> plus L3 and the M4 nit). **H2 was investigated empirically and retracted — it is
> not a bug** (evidence in the H2 section; pinned by a regression test). Tests pass
> 61/61, `npm run lint` is error-free, and the production build succeeds. Items
> still requiring a scope/methodology decision — **M1** (removing the orphaned
> backend), the algorithmic half of **H3** (porting a calibrated classifier), and
> **P1** (multi-mouse prediction) — are left open for the maintainer. Each finding
> is annotated **[FIXED]**, **[OPEN]**, or **[RETRACTED]** below.

---

## Executive summary

The project is in good overall health. The analysis engine is well factored into
pure, testable modules; t-SNE correctly standardizes features before embedding;
Welch's t-test and its Welch–Satterthwaite degrees of freedom are implemented
correctly; clustering uses a seeded PRNG for reproducibility; and there is no
dangerous DOM injection anywhere (`dangerouslySetInnerHTML`, `eval`, `innerHTML`
all absent). All 54 unit tests pass.

The findings below are ranked by impact. The highest-value **confirmed** item is a
**scientific-correctness** issue in a reported metric (the density similarity
score, H1 — fixed), not a crash: it silently misled the user. A second suspected
correctness issue (t-SNE perplexity selection, H2) was investigated empirically
and **retracted** — the feared behavior does not occur. The rest are maintainability
and hygiene items, historically dominated by a **fully orphaned Python backend**
that duplicates the JS engine and can drift out of sync.

---

## High-impact findings (methodology / correctness)

### H1. [FIXED] Density "similarity score" had a floor of ~31% and never reached 0

`frontend/src/engine/prediction.js:75-83` (and the mirror in
`backend/core/prediction.py:56-57`).

Jensen–Shannon divergence computed with the **natural log** is bounded to
`[0, ln 2]` ≈ `[0, 0.693]`. The code reports
`similarityScore = (1 - jsDivergence) * 100`, so two **completely disjoint**
distributions score `(1 − 0.693) × 100 ≈ 30.7`, not 0. The full 0–100 range is
never used; every score is compressed into ~31–100 and reads as far more
"similar" than it is.

**Fix:** normalize JSD by `ln 2` before inverting:
`similarity = (1 − jsDivergence / Math.LN2) * 100`. Then identical → 100,
disjoint → 0. (The Python backend squares the scipy JS *distance*, which has the
same `ln 2` ceiling and the same floor bug.)

### H2. [RETRACTED — investigated empirically, not a bug]

`frontend/src/engine/tsne.js:241-252` selects the perplexity whose run has the
lowest final KL. The first-pass **hypothesis** was that this biases toward the
*smallest* perplexity (a sparser target `P` having a lower achievable KL), which
would defeat the sweep.

**This hypothesis was tested and is false.** Instrumenting `tsneRun` and measuring
the final KL for every candidate perplexity, on both synthetic 3-blob data and the
real reference CSVs:

| dataset (n unique) | candidates | KL by perplexity | picked |
|---|---|---|---|
| synthetic blobs (60) | 5,10,30,50 | 0.586 / 0.376 / **0.234** / 0.427 | 30 |
| synthetic blobs (150) | 5,10,30,50,100 | 1.186 / 0.903 / 0.311 / 0.428 / **0.309** | 100 |
| who_human_fertile (465) | 23,46 | 0.709 / **0.389** | 46 |
| who_human_subfertile (445) | 22,44 | 0.718 / **0.343** | 44 |

In every case the **smallest** perplexity has the **highest** KL, so min-KL
selection favors a moderate/large perplexity — a sensible default, the opposite of
the feared degenerate case. The theoretical note that KL is not strictly
comparable across different target `P` stands, but the practical effect is benign.

Codified as a regression guard: `__tests__/engine/tsne.test.js` →
"min-KL selection does not collapse onto the smallest perplexity" (also asserts the
smallest perplexity is the worst). No code change made; this finding is withdrawn.

### H3. [OPEN] Cluster-proportion classifier confidence is arbitrary

`frontend/src/engine/prediction.js:27-42`. Class probabilities come from a softmax
over **raw, unscaled** Euclidean distances between percentage vectors. The
resulting "probability" has no calibrated meaning — its sharpness depends entirely
on the arbitrary units/scale of the proportion space. It is fine as a nearest-
centroid *label*, but the reported `probability` should be presented as a relative
score, not a confidence. (The backend's Model A uses a real `LogisticRegression`,
so the JS port silently changed the model.)

### H4. [FIXED] No multiple-comparison correction on group comparisons

`frontend/src/engine/exporter.js:44` flags `significant: pValue < 0.05` per
parameter, and the UI runs Welch's t-test across up to 8 CASA parameters
simultaneously. With 8 independent tests the family-wise false-positive rate is
~34%. Consider Holm–Bonferroni or Benjamini–Hochberg, or at minimum a UI note that
p-values are uncorrected.

---

## Medium-impact findings (maintainability / drift risk)

### M1. [OPEN] The entire FastAPI backend is orphaned dead code

`backend/` (~1000 LoC + its own test suite), `docker-compose.yml`, and
`backend/Dockerfile` are no longer used. Deployment is a **static GitHub Pages
build of `frontend/` only** (`.github/workflows/deploy.yml` builds `frontend/dist`
and never touches `backend/`), and no frontend code calls `/api`, `axios`, or
`localhost:8000` (verified by grep). The backend reimplements the same statistics/
prediction as the JS engine and has already diverged (H3: LogisticRegression vs
nearest-centroid). This is the single largest maintenance liability: it invites
"fix it in the backend" changes that never reach production.

**Recommendation:** either delete `backend/`, `docker-compose.yml`, and the
backend Dockerfile, or move them under an `archive/` path with a README stating
they are not the deployed system. If kept as a reference implementation, add a
test that asserts parity with the JS engine so drift is caught.

### M2. [FIXED] Two unused runtime dependencies shipped in the bundle

`frontend/package.json:15-16` — `axios` and `druidjs` are declared dependencies
but imported nowhere in `src/` (grep-verified). `axios` was the backend HTTP
client; `druidjs` was presumably an earlier t-SNE library replaced by the
self-contained `engine/tsne.js`. Removing them shrinks `node_modules` and the
audit surface (the install currently reports 22 vulnerabilities, several via
transitive deps).

### M3. [FIXED] `npm run lint` failed — 6 ESLint errors

`eslint .` exits non-zero on unused variables:
- `db/index.js:51` — `raw` destructured but unused (though intentional: it strips
  `raw` from the returned list; rename to `_raw` or use `// eslint-disable`).
- `engine/pipeline.js:3` — `standardize` imported but unused.
- `engine/tsne.js:96` — `sum` assigned but never read.
- `hooks/useEngine.js:14` — `data` destructured but unused; `:88` — `step` unused.

None affect runtime (the Vite build doesn't run ESLint), but a failing lint script
erodes its value as a gate. Clean these up and consider adding lint to CI.

### M4. [PARTIAL] `importProcessed` Group/motile-count consistency

`frontend/src/hooks/useEngine.js:41-77`. It stores `group: meta.genotype`
(line 57) rather than `meta.group`, so the Group field entered in the UI for a
processed import is ignored in favor of genotype. It also sets
`motileSperm: data.length` — every imported row is counted as motile, which is
only correct for already-filtered data and inconsistent with the raw-upload path.
Minor, but affects the numbers shown on the Dashboard.

---

## Low-impact findings (edge cases / polish)

- **L1 — LIN unit inconsistency.** `engine/motility.js:20` normalizes LIN from
  percent to fraction *only inside* `isHyperactivated`. The fertility-axis training
  and co-embedding (`engine/crossSpecies.js`) consume LIN raw. If some datasets
  report LIN as 0–1 and others as 0–100, axis training that pools both conventions
  will produce a meaningless weight for LIN. Per-source standardization masks this
  within co-embedding but not within a single pooled training set. Consider
  normalizing LIN/STR/WOB units once at preprocessing time.

- **L2 — Shift-JIS detection is heuristic.** `engine/preprocessing.js:13-25` decides
  encoding by scanning the first 500 chars for U+FFFD. A UTF-8 file whose garbled
  bytes fall after char 500, or a Shift-JIS file that happens not to produce a
  replacement char early, can be misdetected. Acceptable for the known CASA
  exporters; worth a comment noting the assumption.

- **L3 — `Math.max(...array)` on large arrays.** `engine/prediction.js:48-50` and
  the softmax spread calls use `Math.max(...all.map(...))`. For very large
  embeddings this can hit the argument-count limit and throw. t-SNE is capped well
  below that today, but a reduce-based min/max is safer.

- **L4 — React hooks exhaustive-deps warnings.** Seven `react-hooks/exhaustive-deps`
  warnings across the pages (`db`/`dispatch` omitted from effect deps). Currently
  harmless because those objects are stable, but they are latent bugs if the hook
  identities ever change.

- **L5 — Feature-tuple dedup in the pipeline.** `engine/pipeline.js:22-33` keys
  uniqueness on `VCL_VSL_ALH_BCF` joined as a string. Correct today (duplicates are
  re-expanded on merge-back so proportions are preserved), but note it assumes
  exact float equality; upstream rounding changes could collapse distinct cells.

---

## What is solid (verified, no action needed)

- **Welch's t-test** (`statistics.js:45-57`) — **cross-checked against SciPy's
  `ttest_ind(equal_var=False)`**: t, Welch–Satterthwaite df, and p match to 6
  significant figures on three cases (incl. a zero-variance edge → t=0, p=1).
- **t-SNE** standardizes inputs (`tsne.js:215`), uses early exaggeration, a
  momentum schedule, adaptive gains, and a seeded PRNG for determinism.
- **K-means** (`clustering.js`) — z-score standardization, `nInit` restarts keeping
  lowest inertia, empty-cluster guard, seeded init.
- **Security surface** — pure static client-side app; no `eval`, no `innerHTML`, no
  network writes, data confined to IndexedDB. CSV is parsed by PapaParse and never
  interpolated into the DOM.
- **Tests** — 61/61 pass, covering config, preprocessing, clustering, t-SNE (incl.
  the H2 selection guard), statistics (incl. BH adjustment), motility, exporter,
  group comparison, cross-species, prediction (incl. the normalized density score),
  pipeline, and the IndexedDB layer.

---

## Verification ledger (closed-loop)

Every material claim in this review was driven to an evidence-backed verdict, not
left as assertion. The one hypothesis that failed its experiment (H2) is corrected
in place rather than quietly dropped.

| Claim | How it was verified | Verdict |
|---|---|---|
| H1 — density score floored at ~31% | Numeric test: disjoint → <1, identical → >99.9 (was ~31 / 100) | **Confirmed → fixed** |
| H2 — min-KL picks smallest perplexity | Instrumented `tsneRun`; measured KL per perplexity on synthetic + real data | **Refuted** — smallest has the *highest* KL; retracted, guarded by test |
| H3 — classifier "confidence" is uncalibrated | Ran classifier on identical data in percent vs fraction units | **Confirmed** — 100% vs 61.9% for the same ranking → relabeled |
| H4 — no multiple-comparison correction | Added BH; unit-tested against R's `p.adjust(method="BH")` | **Confirmed → fixed** |
| D1 — reference VCL descriptors wrong | Recomputed stats over the shipped CSVs with the app's own classifier | **Confirmed → docs corrected** |
| Welch t-test correctness | Cross-checked t/df/p vs SciPy on 3 cases (6 sig figs) | **Confirmed solid** |
| P2/P3 — silent-failure surfaces | Code path traced; guarded with up-front validation / match-rate reporting | **Confirmed → fixed** |

---

## Second-pass deep-dive (pages, DB, prediction UI, import script, reference data)

A follow-up pass read the parts the first pass skimmed and empirically audited the
shipped reference data by running the app's own `classifyWHO` / `isHyperactivated`
over both CSVs.

### D1. [FIXED] Built-in reference descriptors overstated VCL (and hyperactivation)

`frontend/public/references/manifest.json` and the README claimed the fertile set
was `VCL≈95±18 µm/s, hyperactivated≈10%` and the subfertile set `VCL≈55±18 µm/s,
hyperactivated≈3%`. Measured on the actual shipped rows:

| Metric | Fertile (claimed → actual) | Subfertile (claimed → actual) |
|---|---|---|
| VCL mean±SD | 95±18 → **66±56** | 55±18 → **23±31** |
| PR% | 55 → 53 ✓ | 22 → 22 ✓ |
| Hyperactivated% | 10 → 6.5 | 3 → 1.6 |
| IM% | — → 25 | — → 49 |

The **PR% labels that drive the fertile-vs-subfertile contrast are correct**, so the
data is fine for its purpose — but the VCL descriptors were off by ~30 µm/s in the
mean and ~3× in the SD (the real data is a heavy-tailed motile+immotile mixture,
min VCL = 0, not a tight Gaussian). Fixed by correcting the manifest and README to
the measured values rather than regenerating the data (which would change the
labels the tests and users rely on).

### P1. [OPEN] Cluster-proportion prediction silently classifies only the first mouse

`frontend/src/hooks/useEngine.js:145-149`. `predict('cluster_classifier', …)` does
`const firstMouse = Object.keys(sampleProps)[0]` and classifies only that mouse.
For a dataset containing multiple mice (e.g. a processed import spanning several
animals) every mouse after the first is silently ignored, with no UI indication.
Fine for the common one-dataset-per-mouse workflow; a landmine otherwise. Consider
aggregating across mice or exposing a mouse picker.

### P2. [FIXED] Reference-CSV parsing assumes cluster columns are literally `0..4`

`frontend/src/pages/Prediction.jsx:38` builds reference proportions from
`{ 0: r[0], 1: r[1], … 4: r[4] }`, hard-coding five clusters named `0`–`4`. A
reference CSV whose cluster columns are named differently (or that has ≠5 clusters)
silently yields all-zero proportions and a meaningless nearest-centroid result. At
minimum, validate the header and surface a clear error.

### P3. [FIXED] VISEM importer group labels depend on an unchecked ID join

`scripts/import_visem.mjs:161-195`. Group (`normal`/`subnormal`) comes from
`participantPR[pid]`, where `pid` is a `videos/<id>` directory name and
`participantPR` is keyed by `ID`/`id`/`participant_id` from
`semen_analysis_data.csv`. If those two identifier formats differ (zero-padding,
prefixes), every participant silently falls to `group: "unknown"`. The script warns
only when the aggregate file is *missing*, not when the join misses. Consider
reporting the match rate (e.g. "matched PR for 60/85 participants").

### Additional solid spots verified this pass

- **App bootstrap** (`App.jsx`) — `ErrorBoundary` around the tree, lazy-loaded
  routes, and a one-time `StartupLoader` that idempotently seeds the references
  (keyed on `mouseId`, cancels on unmount).
- **IndexedDB layer** (`db/index.js`) — dataset+results deletion is transactional;
  `getDatasets` strips the heavy `raw` CSV from list responses.
- **VISEM importer** correctly stamps `Type: 99`, frame-averages to synthetic cells,
  and maps a broad set of column aliases.

---

## Suggested priority order

**Already applied on this branch:** H1, H4, M2, M3, D1, P2, P3, plus L3 and the M4 nit.

**Investigated and retracted:** H2 (t-SNE selection) — empirically not a bug.

**Remaining, for maintainer decision:**
1. **M1** (remove/quarantine the backend) — biggest drift/maintenance win.
2. **H3 (algorithmic half)** — optionally port a calibrated classifier
   (logistic regression) in place of the nearest-centroid softmax. The UI is
   already relabeled so the current number isn't over-interpreted.
3. **P1** — prediction robustness: multi-mouse classification currently uses
   only the first mouse (needs a behaviour decision).
4. **L1, L2, L4, L5** — edge-case polish as capacity allows.
